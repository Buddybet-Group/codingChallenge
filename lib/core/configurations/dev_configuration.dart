import 'configuration.dart';

/// The app configuration for the Dev Build. It is the same as the base configuration.
class DevConfiguration extends Configuration {
  /// Constructs a new Configuration object with the given environment.
  const DevConfiguration();
}
