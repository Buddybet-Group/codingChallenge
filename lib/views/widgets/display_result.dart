import 'package:flutter/material.dart';

class SearchResultDisplay extends StatelessWidget {
  const SearchResultDisplay({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      
    );
  }
}